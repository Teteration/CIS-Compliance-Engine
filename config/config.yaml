# CIS Compliance Engine Configuration

# Define your target environment here
target:
  # Driver type matches the filename in src/drivers/ (e.g., 'oracle', 'linux')
  driver: "oracle"
  host: "172.24.39.71"
  
  # Connection details specific to the driver
  connection:
    port: 1521
    service_name: "pushrcvrdb"
    user: "pentest"
    password: "pentest"

  # Optional SSH access for OS-level checks (extproc, file perms)
  ssh:
    enabled: true
    user: "oracle"
    port: 22
    password: "P@ssw0rdP@ssw0rd"

audit:
  # The benchmark file to parse
  benchmark_pdf: "CIS_Oracle_Database_19c_Benchmark_v1.2.0.pdf"
  # Cache parsed rules to speed up subsequent runs
  rules_cache: "rules_cache.json"

reporting:
  output_dir: "reports"
  # formats: json, text, or both
  format: "both"